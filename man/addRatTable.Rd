% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coreFunctions.R, R/zzz_aliases.R
\name{addRatTable}
\alias{addRatTable}
\alias{lpr_add_table}
\title{Add Supplementary Table to locusPackRat Project}
\usage{
addRatTable(
  data,
  table_name,
  link_type = c("gene", "region", "point"),
  link_by = NULL,
  abbreviation = NULL,
  project_dir = "."
)

lpr_add_table(
  data,
  table_name,
  link_type = c("gene", "region", "point"),
  link_by = NULL,
  abbreviation = NULL,
  project_dir = "."
)
}
\arguments{
\item{data}{data.frame or data.table with supplementary information}

\item{table_name}{Character: Name for the output table (without .csv extension)}

\item{link_type}{Character: "gene", "region", or "point".
"point" treats data as genomic positions (SNPs) and maps them to overlapping regions.}

\item{link_by}{Character: Column name in data to use for linking.}

\item{abbreviation}{Character: Optional short prefix (e.g., "mm", "otd") used to
distinguish columns from this table when exporting to Excel.
If another table has the same column name, this prefix will
be prepended (e.g., "mm_description"). Must be unique per project.}

\item{project_dir}{Character: Path to project directory containing .locusPackRat}
}
\value{
Invisible TRUE on success
}
\description{
Adds supplementary data tables linked to genes or regions in an existing project.
Can handle gene-based data, genomic regions, or point coordinates (e.g., SNPs).
}
\examples{
\dontrun{
# Link by gene symbol
expression_data <- data.frame(
  gene_symbol = c("Myc", "Tp53"),
  tissue_expr = c(100, 50)
)
addRatTable(expression_data, table_name = "expression",
            link_type = "gene", link_by = "gene_symbol",
            project_dir = "my_analysis")

# Link by genomic region
peak_data <- data.frame(
  chr = c("1", "1"), start = c(1000, 2000), end = c(1500, 2500),
  score = c(10, 20)
)
addRatTable(peak_data, table_name = "peaks", abbreviation = "pk",
            link_type = "region", link_by = "chr,start,end",
            project_dir = "my_analysis")
}

}
